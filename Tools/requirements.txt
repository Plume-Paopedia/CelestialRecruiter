# CelestialRecruiter Tools - Python dependencies

# Required (Discord webhook companion + License bot)
requests>=2.28.0    # For sending HTTP requests to Discord
flask>=3.0.0        # For Patreon webhook server (license bot)

# Optional but recommended
watchdog>=2.1.0     # For file system monitoring (more efficient than polling)
